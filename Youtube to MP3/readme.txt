python modules:

pip install pytube && pip install moviepy

after install the modules, import the downloaded modules pytube and moviepy to our python file
use command

from pytube import YouTube
from moviepy.editor import *